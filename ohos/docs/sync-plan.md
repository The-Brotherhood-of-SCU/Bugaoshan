# 鸿蒙端上游同步工作计划

更新日期：2026-09-17。状态：阶段一和阶段二原实现已完成，阶段二真机基础流程已通过。
前两阶段及第三阶段源码适配均保存在 OH 补丁中。用户于 2026-09-16 确认下列四项第三阶段
真机功能验证通过，已据此勾选；代理后续仅修改 OH 代码补丁和文档，没有构建或测试。
第三阶段常用平台能力和业务流程已收口，旧凭据处理代码完成；旧版本覆盖升级留在阶段七。
2026-09-16 用户进一步确认本次 WebView 原生主题修复“正常运行”：主题变化更新现有节点，
ArkWeb AUTO 配合现有深浅 CSS，不再因主题变化主动刷新或重建 WebView。该问题记为通过，
验收依据为用户反馈，代理未执行构建或测试。
2026-09-16 用户确认第四阶段剩余部分均无问题，附件与验证码、通知导航与布局、志愿四川、
主题与滚动补充场景全部记为通过，第四阶段收口。记录见 [第四阶段收尾清单](phases/phase4.md)，
技术依据见 [通知页排查](audits/notice-webview.md)。第五至七阶段仍按原计划推进。

目标是让鸿蒙端运行当前上游的共享业务代码，并建立持续同步和验证流程。当前共享的 `lib/` 已经是上游代码，主要工作集中在 SDK 兼容、鸿蒙插件、原生能力和运行验证。

## 1. 维护约定

- 所有平台在同一分支维护，不建立独立的鸿蒙分支。
- 共用根目录 `lib/`、`assets/` 和 `pubspec.yaml`；只为有鸿蒙适配的 Dart 文件维护完整覆盖版本。
- 鸿蒙工作的持久修改范围严格限定为 `ohos/`，前两阶段亦适用；相关脚本统一放在 `ohos/tool/`。
- Dart 适配保存为 `ohos/flutter/overrides/lib/` 下的完整文件，翻译差异放在 `ohos/flutter/l10n/`；
  `source-manifest.json` 登记上游基线，组装前检查。根源码、文档和测试保持上游状态。
- 代理当前只修改代码和文档，测试、依赖解析、构建及真机调试由用户执行。
- 鸿蒙只使用正式稳定版 Flutter OH SDK，不使用 canary、beta、dev 等预览版本。
- 其他平台继续使用上游 Flutter 环境；鸿蒙专用依赖配置和锁文件放在 `ohos/flutter/`。
- 鸿蒙依赖解析、代码生成和构建在独立副本中执行，避免改动根工程的锁文件和生成物。
- 本文用于跟踪同步工作。只有取得对应验证证据后，才勾选完成项；构建方法以 [鸿蒙开发说明](../README.md) 为准。

## 2. 本次同步基线

以下信息是调查时的快照。后续同步应重新记录目标提交，不以本文版本号作为永久构建配置。

| 项目 | 调查结果 |
| --- | --- |
| 上游仓库 | `https://github.com/The-Brotherhood-of-SCU/Bugaoshan.git` |
| 本次目标 | `main@e5c38070e154ab1a62b701534055e03ab9c8861f`，提交日期 2026-09-13；调查时与本地 HEAD 一致 |
| 上游版本声明 | 根 `pubspec.yaml` 为 `2.5.1+20501` |
| 目标提交的标签 | HEAD 同时带有 `v2.5.2-preview` 标签，因此使用具体提交号标识本次目标 |
| 旧鸿蒙版本声明 | `ohos/AppScope/app.json5` 为 `2.2.0` / versionCode `3`；`ohos/oh-package.json5` 为 `2.2.0` |
| 对照历史版本 | `v2.2.0`，提交 `e02eada`，发布日期 2026-07-13 |
| 上游 SDK | CI 使用 Flutter `3.44.9`；根锁文件要求 Dart `>=3.12.0` |
| 本机 OH SDK 报告值 | Flutter `3.41.10-ohos-1.0.0` / Dart `3.11.5` |
| 当前 OH SDK | 正式 tag `3.41.10-ohos-1.0.1`，提交 `adaf911c35c9136a7d18fc424d714c9ec7724e60`，已验证 |
| 当前 API 工具链 | DevEco Studio `26.0.0.821`、API 26 SDK `26.0.0.105`、Hvigor `6.26.4`、ohpm `26.0.0.630`、Node.js `v24.14.1` |

从 `v2.2.0` 到本次目标共有 366 个非合并提交，477 个文件变化，新增 53,718 行、删除 9,668 行。这是全仓库的差异规模，不是鸿蒙需要重新实现的工作量。

旧 `ohos/` 尚无可追溯的已提交历史，不能仅凭版本声明确定它当时对应的完整 Dart 源码提交。OH Flutter 工具还会在构建时更新应用版本，因此静态配置中的 `2.2.0` 也不能独立证明最终 HAP 的版本。

## 3. 当前完成情况

以下自动化检查和编译记录保留迁移前的历史证据；第三阶段四项功能的最新验收依据是
2026-09-16 用户的真机反馈，不代表代理重新执行了检查，也不替代构建流程专项验证。
2026-09-17 的新入口直接使用根 `ohos/`，共享源码通过 `.flutter-workspace/` 链接；
DevEco Sync 已接入首次自动准备和失效运行环境重建。这次入口改造只交付代码，
依赖解析、代码生成、构建和真机验证由用户执行，尚未标记通过。

- [x] 确定同分支、共享源码、鸿蒙独立依赖锁的目录方案。
- [x] 创建 `ohos/flutter/pubspec_overrides.yaml` 和 `ohos/flutter/pubspec.lock`。
- [x] 建立 `ohos/tool/build_ohos.py`，支持独立副本、严格锁定解析、显式更新锁文件和 HAP 构建入口。
- [x] 使用当前本机 OH SDK 执行 `--prepare-only`，严格依赖解析通过。
- [x] 构建脚本 18 个测试、补丁校验 1 个测试、8 个 Dart 适配/下载处理测试及 3 个主题转场测试通过。
- [x] 固定并验证正式 SDK tag、提交和 API 26 配套工具链。
- [x] 完成前两阶段源码兼容修复，并在迁移前通过 Flutter OH SDK 的格式、分析、测试和 HAP 编译检查。
- [x] 替换九项 CPF 依赖，完成后三项迁移，验证全部 13 项 OH 插件注册与无签名 HAP 构建。
- [x] 完成 unsigned HAP 构建，并核对包名、版本和目标 API。
- [x] 完成真机安装、冷启动、登录、课表加载及退出后重新打开验证。
- [x] 将前两阶段根源码改动和 OH 覆盖实现迁为 4 组源码补丁，恢复范围外文件；文档及 Python 测试移入 `ohos/`。
- [x] 用户已使用迁移后的补丁与隔离副本完成构建和运行；依据后续构建反馈及 2026-09-16 原生主题修复运行确认，不代表专项自动化检查全部完成。
- [x] 第三阶段代码以 `0005` 至 `0012` 源码补丁、4 项插件补丁及日历原生通道异常处理完成落盘。
- [x] 由用户验证第三阶段下列四项平台能力与业务运行结果（2026-09-16 真机反馈）。
- [x] 历史迁移：将构建及依赖清单脚本迁至 `ohos/tool/`，当时构建副本和专用缓存为 `ohos/build/workspace/` 与 `ohos/build/pub-cache/`；当前路径见下表。
- [x] 用户已使用 `ohos/tool/build_ohos.py` 和 `ohos/build/workspace/` 新路径构建、调试；依据会话中的构建输出及后续运行反馈。
- [x] 完成第四阶段通知页、附件与验证码、志愿四川、主题及滚动专项验证（2026-09-16 用户确认）。
- [x] 完成第五阶段课表卡片、动态图标、环境信息及开发者页调整（2026-09-16 用户整体验收确认）。
- [x] 完成维护目录整理：文档归入 `ohos/docs/`，插件补丁归入 `ohos/flutter/patches/plugins/`，
  测试归入 `ohos/tests/`；脚本、文档链接及测试模板准备逻辑已同步，迁移后的构建和测试待用户执行。
- [x] 按用户新方案将 26 个源码补丁转换为 65 个完整 Dart 文件及两份 ARB 差异条目，
  构建入口改为检查基线后复制覆盖和合并；详情见 [迁移记录](audits/source-overlay-migration.md)。
- [ ] 用户执行文件覆盖迁移后的源码检查、格式、分析、测试和 HAP 构建；历史通过记录不代替本次验收。
- [x] 将根 `ohos/` 接为唯一原生工程；共用源码改用文件链接，Flutter 解析和生成代码独立放在
  `.flutter-workspace/`，Pub 缓存放在 `.pub-cache/`，接入本地 Hvigor 路径适配。
- [x] 精简源码覆盖：提交 `0fd213a` 后覆盖共 67 个；2026-09-19 提交 `f7e0933` 取消
  23 个已可改用上游实现的覆盖，降至 44 个；其后移除与上游逐字节一致的 `repair_page.dart`。
  当前为 28 个上游覆盖 + 15 个 OH 新增，共 43 个 Dart 文件；数量以
  `ohos_sources.py --check` 的输出为准。
- [x] 将首次准备和依赖/路径失效后的重建接入 DevEco Sync，保留 `--prepare-only` 作为手动入口。
- [x] 提交无签名 `build-profile.json5` 基线，使 DevEco 在首次 Sync 前能识别根 `ohos/` 工程。
- [ ] 用户执行根 `ohos/` 首次 DevEco Sync、Build 和真机验证。
- [ ] 完成第七阶段旧版本覆盖升级验证。

当前九项 CPF 插件和后三项迁移均已接入：文件保存使用 `file_picker_ohos 10.3.8`，
相册保存使用 `image_gallery_saver_plus 3.0.5`，WebView 使用 CPF `6.1.5` / OH `1.1.3`。
鸿蒙依赖配置与补丁位于 `ohos/flutter/`，测试位于 `ohos/tests/`，阶段与排查文档位于
`ohos/docs/`；目录职责见 [开发入口](../README.md) 和 [适配说明](flutter-adaptation.md)。
`win32` 临时覆盖已移除，依赖自然解析到 5.15.0；鸿蒙锁共 194 包，与根锁有 43 项不同、
21 项仅根锁存在、10 项仅 OH 锁存在。完整对照见 [完整依赖对照](dependencies/lock-inventory.md)。

当前 `plugins.ohos` 和 ArkTS 注册文件包含全部 13 项：`file_picker_ohos`、
`flutter_inappwebview_ohos`、`image_gallery_saver_plus`、`flutter_secure_storage_ohos`、
`image_picker_ohos`、`open_filex`、`os_type`、`package_info_plus`、`path_provider_ohos`、
`share_plus`、`shared_preferences_ohos`、`sqflite_ohos`、`url_launcher_ohos`。
旧注册文件不能作为插件已经接入的依据，也不能靠手工维护该生成物完成适配。

## 4. 文件职责

| 位置 | 职责 |
| --- | --- |
| [根 pubspec.yaml](../../pubspec.yaml) | 共用的应用版本、资源和依赖声明 |
| [根 pubspec.lock](../../pubspec.lock) | 上游 Flutter 环境的依赖锁 |
| [依赖覆盖](../flutter/pubspec_overrides.yaml) | 鸿蒙专用插件替换和兼容覆盖 |
| [依赖增减配置](../flutter/pubspec_dependencies.json) | 只在隔离构建副本中注入或排除的直接依赖 |
| [Dart 覆盖文件](../flutter/overrides/README.md) | 适配后的完整文件，按同一路径复制到 OH 副本 |
| [源码清单](../flutter/source-manifest.json)及[翻译条目](../flutter/l10n/README.md) | 文件基线检查及 ARB 按键合并 |
| [Python 测试](../tests/python/) | 构建及补丁脚本测试 |
| [Flutter 测试模板](../tests/flutter/) | 仅在 OH 副本还原为 Dart 文件的专项测试 |
| [插件补丁](../flutter/patches/plugins/) | 原生插件补丁、版本清单和旧缓存迁移依据 |
| [鸿蒙依赖锁](../flutter/pubspec.lock) | 鸿蒙 SDK 对应的完整依赖锁 |
| [工具链锁](../flutter/toolchain.lock.json) | 正式 Flutter OH、API 26 和 DevEco 工具版本锁定，不含本机路径 |
| [依赖说明](dependencies/overview.md) | 直接依赖、OH 插件需求、OHPM 与工具链分类清单 |
| [依赖替代矩阵](dependencies/replacements.md) | 全部直接依赖的 CPF 稳定替代、迁移和排除结论 |
| [完整依赖对照](dependencies/lock-inventory.md) | 根锁与鸿蒙锁的全部包、版本、来源和差异状态 |
| [原生源码与资源](../entry/src/main/) | ArkTS 入口、原生通道、页面、权限和资源 |
| [应用配置](../AppScope/) | 应用标识、版本字段和应用资源 |
| [ohos/tool/build_ohos.py](../tool/build_ohos.py) | 鸿蒙依赖准备与构建入口 |
| [ohos/tool/ohos_sources.py](../tool/ohos_sources.py) | 上游基线检查、Dart 覆盖和 ARB 合并 |
| [ohos/tool/ohos_patches.py](../tool/ohos_patches.py) | 第三方插件补丁应用逻辑 |
| [ohos/tool/generate_ohos_dependency_inventory.py](../tool/generate_ohos_dependency_inventory.py) | 从两份锁文件重建完整 Dart 依赖对照表 |
| [开发入口](../README.md) | 鸿蒙开发环境、构建及 DevEco 调试说明 |
| `ohos/.flutter-workspace/` | 链接共用源码的鸿蒙编译工程，依赖和生成物独立，不提交 |
| `ohos/.pub-cache/` | 鸿蒙专用 Pub 缓存与插件补丁应用位置，不提交 |
| `ohos/` | 唯一原生工程及 DevEco 打开位置，不再复制到 Flutter 工作目录 |

`ohos/flutter/` 下的覆盖配置不会被根工程自动采用。构建脚本将它和鸿蒙锁文件复制到构建副本根目录后，Pub 才按这套配置解析。

## 5. 分阶段实施

### 阶段一：固定正式 SDK 和原生构建基线

- [x] 固定正式 `3.41.10-ohos-1.0.1` tag 和对应提交，记录 framework、engine、Dart 版本。
- [x] 在 `ohos/flutter/` 增加机器可读的 SDK 固定配置，并让构建脚本验证实际 SDK tag、提交、engine 和 Dart 版本。
- [x] 核对 DevEco、HarmonyOS SDK、Hvigor 和工程 API 配置：API 26 原生编译已通过。
- [x] 用固定 SDK 在临时目录生成最小 OH 工程并对照现有工程；包名、资源和已有日历通道均保留。
- [x] 核对 `buildinfo.json5` 等资源位置；正式 SDK 的 Hvigor 插件兼容现有 `rawfile` 位置，无需整体覆盖原生工程。
- [x] 从根 `pubspec.yaml` 获取版本，验证构建副本和最终 HAP 的 versionName / versionCode；`2.5.1+20501` 高于旧安装包的 `2.2.0+3`。
- [x] 构建前从源仓库获取并注入 `GIT_TAG`、`GIT_COMMIT`、`GIT_COMMIT_DATE`，避免不含 `.git` 的副本丢失构建信息。
- [x] 固定 SDK 后重新严格验证鸿蒙依赖锁，无需更新锁文件。

验收：正式 SDK 来源、干净副本依赖解析和 API 26 原生编译已通过，工具链版本可追溯。项目已生成 bundleName 为 `com.scubrotherhood.bugaoshan`、版本为 `2.5.1+20501`、`target=26` 的 unsigned HAP；仓库中的旧鸿蒙版本声明保持不变。

### 阶段二：共享代码兼容、基础插件和构建基线

- [x] 用 [SDK 适配文件](../flutter/overrides/lib/pages/settings/set_dock_page.dart) 处理排序 `onReorderItem` API 差异，正确调整旧回调向下移动时的索引偏移。
- [x] 在同一补丁中处理主题 import / lint 差异，使用带原因的局部忽略。
- [x] 兼容实现统一维护在 `ohos/`，后续按新方案迁为 `ohos/flutter/overrides/lib/` 完整文件，只组装到构建副本。
- [x] 接入 SharedPreferences、path_provider、sqflite、package_info_plus、flutter_secure_storage，并验证生成注册结果。
- [x] 将 url_launcher、image_picker、share_plus、open_filex 同时切至 CPF 稳定来源，固定完整提交并验证生成注册结果。
- [x] `flutter_secure_storage` 依照 EasyNode 的直接 OH 依赖、Git 覆盖和解析后补丁方式接入；依赖解析、OH 注册及 HAP 构建通过，登录及存储相关真机流程于 2026-09-16 由用户确认通过。主包与 OH 实现均固定到正式 tag `9.2.4-ohos-1.0.0` 的提交 `ecc4257040163da3c4dd64d4fced5d4d24676a53`。
- [x] 从 OH 构建副本排除 `device_info_plus`，用补丁引入适配接口；第五阶段接入鸿蒙原生设备信息，Android/iOS 保持上游源码和依赖。
- [x] 从 OH 构建副本排除 `window_manager` 和 `screen_retriever`，以补丁隔离桌面插件 import；窗口状态服务不执行操作，退出服务保留移动端行为，根服务不变。
- [x] `share_plus` 已固定 CPF 非 dev 稳定分支的 12.0.1 提交，OH 注册及 HAP 构建通过；平台调用真机流程于 2026-09-16 由用户确认通过。
- [x] 逐项核对主包、平台接口和 OH 实现的版本及编译 API；优先独立平台实现，必要时对鸿蒙使用固定提交的 fork。
- [x] 迁移前已完成源码适配的格式化、OH 代码生成、静态分析和相关 Flutter 测试；补丁迁移后的验证另行记录。
- [x] 构建项目 unsigned release HAP，并校验包内版本、bundleName 和目标 API。
- [x] 完成真机安装、冷启动、登录、课表加载及退出后重新打开。

安全存储采用同一正式提交中的主包 9.2.4、OH 实现 1.2.2 和兼容的
`flutter_secure_storage_platform_interface 1.1.2`。鸿蒙构建会将 `_selectOptions()` 的未知平台分支改为空 options，
并为 9.2.4 增加 `usesDataProtectionKeychain` 构造参数别名，使共享源码仍保持 10.x API。
根依赖与锁文件继续使用 10.x，因此 Android、iOS 和桌面端不受该补丁影响。
此前安全存储、分享和包信息的 Windows 代码与 `windows_file_picker` 对 win32 的主版本要求
冲突。迁移 file_picker 后，后者已从 OH 依赖图移除，取消强制覆盖即可自然解析 win32 5.15.0。
当前 Dart AOT 与全部 OH 原生插件编译已通过。

文件/相册保存接口及 `DownloadWebView` 均从源码覆盖目录复制到副本，配套调用处采用同路径适配文件，
根源码不保存这些适配文件。Cookie、HTTP 下载、任务状态及弹窗生命周期继续来自上游。
原生插件补丁修复文件选择器忽略本次 bytes，以及相册插件并发/异常不返回结果的问题。
插件补丁应用到 `ohos/.pub-cache/`，源码覆盖及翻译合并只写入构建副本；文件都位于
`ohos/flutter/patches/`。版本、提交或上下文不匹配会停止构建。

补丁迁移前的验证记录：严格锁定解析、代码生成、OH 副本全部 `lib/` 与专用测试静态分析通过；
19 个 Python 测试、8 个 Dart 文件/相册及下载处理测试、3 个主题转场测试通过。
历史副本 `build/ohos-workspace/run-ys4hv2r0/` 曾生成约 33.5 MiB 的 unsigned release HAP，
版本为 `2.5.1+20501`，bundleName 为 `com.scubrotherhood.bugaoshan`，target API 26。
该副本已按用户要求清理；历史构建日志现位于 `ohos/build/ohos-adapters-final-build.log`，
历史测试日志现位于 `ohos/build/ohos-adapters-tests.log`，均为本地忽略文件。
根 `pubspec.yaml` / `pubspec.lock` 哈希保持一致。

按当前安排，上游 Flutter 3.44 SDK 检查移至阶段六，不作为阶段二收口条件。
2026-09-15 用户确认 API 26 真机安装、冷启动、登录、课表加载及退出后重新打开通过。
当时尚未完成其他平台能力、核心业务和覆盖升级验证；第三阶段功能的后续验收见下文。CPF WebView 的
Windows 默认插件声明会产生非阻塞提示，其 ArkTS 源码也有编译警告；无签名 HAP 已实际产出。
第三阶段随后为默认外链调用编写 external 模式补丁，平台流程已获用户真机确认；此前构建成功本身不代表完整功能验收。

验收：共享兼容、依赖、插件注册、OH 静态检查、相关测试和项目 HAP 构建通过；
真机安装、冷启动、登录、课表加载及退出后重新打开通过。

### 阶段三：常用平台能力、新业务和旧用户升级

本阶段源码适配现在维护在 `ohos/flutter/overrides/lib/`，原生改动保存在 `ohos/`；
下方根源码链接用于定位上游调用处，不表示允许直接修改这些文件。

- [x] 编写外链、选图、附件打开和分享的源码适配补丁，文件保存及相册保存沿用第二阶段补丁；功能验收依据见下方用户反馈。
- [x] 为默认网页调用指定 external 模式，并处理返回 false 和异常；不依赖 `harmony_browser_page` 宿主页面。
- [x] 用补丁让日历导出逻辑在 OH 调用已有 `importIcsToCalendar` 通道，启用对应 UI；`opened` 不显示“已导入”提示。
- [x] 补齐日历通道同步/异步异常返回，编写 open_filex、share_plus、image_picker_ohos 的结果处理补丁。
- [x] 按系统选择器和 URI 交接方式核对权限；宿主沿用 INTERNET，未新增全量相册读取或日历写入权限。
- [x] 编写 `0008` 至 `0012` 源码补丁：Passpoint 操作与响应处理、登录及 SSO 会话保护、表单提交快照、上传失败和裁剪保存检查。
- [x] 编写 OH 安全存储错误回传与串行操作补丁，保持已有存储名称、前缀和算法，不因错误重置数据。
- [x] 验证授权、拒绝、取消操作、失败提示，以及各平台调用在真机上的完整流程。
- [x] 验证办事大厅、在线报修及照片上传、余额趋势、课程查询、培养方案完成度、背景裁剪等当前业务。
- [x] 验证 Passpoint 的后端 MAC 绑定流程；当前实现是 HTTP 业务接口，不预设其必须新增原生 Wi-Fi 配置能力。
- [x] 验证登录续期、认证失败恢复、退出登录和账号切换，遵循现有业务的数据隔离与共享约定。
- [x] 制定并实现旧凭据处理策略：SCU 保留原键并核对账号绑定，读失败或归属不明时提示重新登录；报修旧无归属 token 按需重新 SSO，保存带账号归属的新记录；不清空全部用户数据。覆盖升级效果待用户验证。

代码变更与权限依据见 [第三阶段代码说明](phases/phase3.md)，五项待办的代码结论及旧数据
处理策略见 [代码审查记录](audits/phase3-code.md)。认证和业务修复只保存在 OH 补丁中；
根源码、数据库路径/schema、依赖版本和锁文件均未修改，代理未执行构建和测试。

2026-09-16 验收记录：用户表示已真机测试一遍、上述四项功能正常，并要求通过项直接勾选。
这些业务继续跟随上游，不另行扩展鸿蒙业务实现。此次反馈不包含旧安装覆盖升级的专项结论；
通知页大小/位置及深色模式问题归入阶段四，不能随第三阶段一并关闭。

阶段总结：常用平台能力、所列业务、Passpoint 和认证四项已获用户真机确认，第三阶段日常功能收口。
旧凭据兼容策略与代码完成；旧 `2.2.0` 安装包保留数据覆盖升级的专项验收统一放在阶段七，
不通过清空数据处理迁移。日历通道返回 `opened` 只代表打开系统接收端，导入仍由用户在接收端确认。

### 阶段四：恢复 WebView 功能

2026-09-16 验收记录：用户确认上轮列出的剩余部分“都没有问题了”，要求标记通过。
以下功能验收项均据此勾选；代理本轮只更新文档，没有执行构建或测试。

- [x] 固定 CPF WebView 6.1.5 / OH 1.1.3，通过平台组件适配下载回调。
- [x] 验证 [共享通知组件](../../lib/widgets/webview/webview_notice_page.dart) 使用的加载回调、JS 注入、JS bridge、Cookie 和导航行为。
- [x] 分别验证教务处、学工部、团委通知的正文美化、附件提取、下载、打开及分享。
- [x] 编写 `0013-tuanwei-mobile-layout.patch`：青春川大移动视口、桌面容器重排、标题换行、图片及宽表格适配，按用户要求关闭手势缩放；布局与加载时序已获用户确认。
- [x] 根据退出重进偶发原网页的反馈更新 `0013`：首帧不透明遮罩、等待 DOM、当前文档完成标记，以及失败/超时后的重试界面。
- [x] 真机验证青春川大大小/位置、禁用缩放、分页返回、正文及附件；代码说明见 [通知页排查](audits/notice-webview.md)。
- [x] 验证青春川大反复退出重进、缓存加载、慢网和失败重试，美化前不显示原网页。
- [x] `0014` 启用 `ForceDark.AUTO`，`0018` 保留该设置，由 ArkWeb 原生跟随系统深浅主题。
- [x] 将 `0014` 的禁用回弹配置下沉到共用 `DownloadWebView`：三个通知页、志愿四川和附件验证码窗口统一使用 `OverScrollMode.NEVER`。
- [x] 在 `0015` 补齐教务处搜索框动态颜色：追加深浅两套 CSS，覆盖上游一次性设置的内联颜色。
- [x] 在 `0015` 接管青春川大通知页、本域名且消息为空的 `onJsBeforeUnload`，返回已处理并允许翻页；保留其他页面和普通确认逻辑。
- [x] 修正共用 WebView 的生命周期：页面存续期间保留同一插件实例，回调转发最新状态，移除通知组件对控制器的重复释放。
- [x] 保留主题变化及原生渲染进程事件日志；`0018` 关闭通知页切换时的 JS 探测，其余页面保留一次性诊断。
- [x] 编写 `0016-notice-layout-before-display.patch`：三个通知页从首帧遮住原网页，等待美化、资源/字体及布局稳定，再经过两个动画帧展示；失败或超时提供重试，不提前露出中间布局。
- [x] 真机验证三个通知页首次打开、分页、搜索、返回和退出重进无原网页/中间布局闪现；慢网、字体和正文图片加载后显示正常，失败可重试。
- [x] `0017` 的主题刷新方案已被 `0018` 替换；保留其手动重试前先绘制遮罩的时序。
- [x] 编写 `0018-notice-native-theme.patch`：ArkWeb 原生 AUTO 配合上游两套 CSS，显式关闭算法强制染色；移除上一版网页属性转换和主题刷新，教务处搜索框沿用动态媒体查询。
- [x] 编写嵌入层 `color-mode-update.patch`：系统主题变化改为对现有 BuilderNode 调用 `updateConfiguration()`；插件补丁继续传递到内部 WebBuilderNode，避免因主题变化重新创建 Web 文档。
- [x] 接入 Hvigor：读取 Flutter 实际选择的 SDK HAR，按版本/源码哈希在副本中应用补丁，以独立依赖 URL 引用；SDK 安装目录不修改。见 [嵌入层补丁](../flutter/patches/embedding/README.md)。
- [x] 本次 WebView 原生主题修复运行通过：用户于 2026-09-16 反馈“这个部分正常运行了”，关闭已显示页面切换深浅模式时刷新、卡住及 Load Failed 的问题。
- [x] 补充确认首次以深浅主题打开、连续快速切换、返回前台及旋转/尺寸变化时的表现。
- [x] 真机验证所有 WebView 顶底边缘无回弹、正常滚动和验证码操作，以及三个通知页的深浅切换、青春川大分页和附件操作。
- [x] 验证附件验证码成功、取消、失败和重试后的下载任务及弹窗状态。
- [x] 验证志愿四川的登录与页面交互，以及返回、刷新、加载失败等路径。
- [x] 将支持判断移入平台组件，OH 副本启用 WebView 页面，真机功能已获用户确认。

验收：三类通知和志愿四川的关键流程真机通过；需要 Cookie 的附件下载可用；失败路径有可理解的提示。

阶段结论：附件和验证码、页面导航与布局、志愿四川、主题及滚动边界均获用户确认，
第四阶段完成。详细记录见 [第四阶段收尾清单](phases/phase4.md)。

### 阶段五：补齐其他鸿蒙原生能力

状态：第五阶段已完成，用户于 2026-09-16 确认“第五阶段没有问题了”；以下功能验收按该反馈更新。
实现与验收清单见 [第五阶段说明](phases/phase5.md)。
课表卡片显示与交互按用户要求对齐安卓统一自适应组件：三种规格展示完整可滚动列表，
不按尺寸固定限制课程条数；具体字段、状态和视觉规则见方案第三节。

- [x] 完成课表服务卡片代码：当前课表展示快照、多实例同步、周期刷新、自适应完整列表和系统管理入口。
- [x] 完成 API 26 官方备用图标切换代码；低版本返回不支持。
- [x] 去掉鸿蒙图标切换确认中的重启文案，保留切换确认。
- [x] 补齐开发者页鸿蒙设备信息、复制全部及读取失败重试代码。
- [x] 移除开发者页 UI Preview 入口、对应分隔线及 import，已获用户验收确认。
- [x] 在能力矩阵中记录代码实现状态及平台限制。
- [x] 用户确认第五阶段构建与功能验收无问题；代码生成、格式、分析的持续检查在阶段六跟踪。
- [x] 课表服务卡片验收完成，依据用户“小组件也补齐了”的反馈更新卡片添加/删除、三种规格、滚动、状态、主题、课表切换/清空与跨日刷新条目。
- [x] 真机验证图标切换、当前选择状态、手动重启后的状态保持及失败提示，确认切换文案不再提示重启；按用户第五阶段整体验收反馈标记。
- [x] 真机验证开发者页设备信息、复制全部与读取失败后的重试；按用户第五阶段整体验收反馈标记。

按用户最新要求，鸿蒙应用内下载更新包和自安装方案已取消，相关通知通道也已撤除。
第五阶段交付课表卡片、动态图标和开发者页环境信息；第七阶段保留发布签名及通过外部安装方式覆盖升级的验证。
通知附件继续使用既有下载流程。

阶段结论：课表卡片、动态图标和开发者页调整已获用户验收确认，第五阶段完成。
代理未执行构建或测试，未另行记录逐项命令结果；持续检查与双 SDK 验证继续在阶段六开展。

### 阶段六：建立持续同步检查

- [ ] 使用上游 Flutter 3.44 SDK 完成代码生成、格式检查、静态分析和 Flutter 测试，确认鸿蒙兼容修改不影响 Android、iOS 和桌面端。
- [ ] 在 `ohos/tool/` 提供独立的依赖解析、代码生成、分析和测试检查入口，配置及测试放在 `ohos/`，使用正式 SDK 和鸿蒙锁文件，供 CI 调用。
- [ ] 在具备原生工具链的环境通过上述入口执行 unsigned HAP 构建；当前修改范围不包含 `.github/` 工作流。
- [ ] 为涉及平台能力、数据库或认证存储的变更安排真机回归，并保存验证记录。
- [ ] 只有未来正式发布的 OH SDK 能满足上游要求且验证通过后，才评估合并 SDK 基线、减少依赖覆盖。

验收：每个候选同步提交都有双 SDK 检查结果；发布候选包有 HAP 构建与必要真机验证结果；能够查到最后一次验证通过的源码及 SDK 提交。

### 阶段七：发布签名与覆盖升级

- [ ] 在 DevEco Studio 中配置发布签名，证书、密码和本机路径均不提交到仓库。
- [ ] 生成签名 HAP，复核包名、versionName、versionCode、证书和发布渠道要求。
- [ ] 用旧 `2.2.0` 安装包覆盖升级到签名 HAP，保持相同包名和签名，检查 SQLite schema `1 → 2`、设置、凭据及文件目录。
- [ ] 验证签名 HAP 的安装、启动、升级和回滚边界，保存最终发布记录。

验收：签名材料留在发布环境；签名 HAP 可以安装和覆盖升级；升级后的课表、设置、凭据和文件目录符合迁移约定。

## 6. 能力追踪矩阵

本表按已有构建记录与 2026-09-16 用户反馈更新；未单独确认的专项场景仍列为待验收。

| 能力 | 当前状态 | 工作阶段 |
| --- | --- | --- |
| 正式 SDK 可复现基线 | 正式 tag、提交和 API 26 工具链已固定并验证 | 一 |
| 独立依赖锁及构建副本 | 严格解析通过，13 项 OH 插件注册，无签名 HAP 构建通过 | 一、二 |
| 设置、数据库、包信息、安全存储 | CPF 接入与构建通过，登录、课表及第三阶段相关业务真机通过；旧安装数据升级另验 | 二、三、七 |
| 冷启动、登录、课表 | API 26 真机安装、冷启动、登录、课表加载及退出后重新打开已通过 | 二 |
| 图片、文件、分享和外链 | 调用层和插件适配完成，平台操作流程已获用户真机确认 | 三 |
| 日历导入 | ICS 交接已纳入用户平台流程通过反馈；`opened` 仍仅表示系统接收请求 | 三 |
| 当前业务与账号会话 | 所列业务、Passpoint、登录续期/恢复/退出/切账号已获用户真机确认 | 三 |
| 旧安装数据升级 | 兼容策略和代码完成；旧包覆盖升级仍待验收 | 三、七 |
| 三类通知、志愿四川 | 原生主题修复及附件/验证码、导航/布局、登录/交互、主题/滚动专项均获用户确认，阶段四完成 | 四 |
| 课表服务卡片 | 三种规格、安卓对齐列表、当前课表快照和系统刷新已交付；用户确认小组件完成，卡片验收已勾选 | 五 |
| 动态图标 | API 26 官方接口及切换确认文案已获用户验收确认；低版本明确不支持 | 五 |
| 开发者页环境信息 | 原生品牌/型号、系统版本、API 和 ABI 查询及复制/重试已获用户验收确认 | 五 |
| 开发者页 UI Preview | 入口已移除，已获用户验收确认 | 五 |
| 鸿蒙应用内自更新 | 按用户要求取消，不提供更新包下载、自安装及配套通知通道 | 不纳入 |
| 持续双 SDK 验证 | Flutter OH 已验证；上游 Flutter 3.44 检查按安排移至阶段六 | 六 |

## 7. 每次同步上游的执行流程

1. 记录上一次验证通过的源码提交、本次目标提交、正式 SDK 提交和鸿蒙锁文件版本。
2. 从同一分支的上游更新中检查四类变化：共享 Dart / Flutter API、插件依赖、原生通道、数据库或持久化格式。
3. 先用当前鸿蒙锁严格解析；如果必须调整依赖，修改鸿蒙覆盖配置，再显式更新锁文件并审查差异。
4. 将兼容修改保存为 `ohos/` 下的补丁，由脚本应用到 OH 副本；不修改根共享代码。由用户执行代码生成、格式、分析和测试，双 SDK 校验按安排在阶段六开展。
5. 构建 HAP，并按变化范围回归真机功能；发布候选包至少验证冷启动、登录、课表、数据持久化和覆盖升级。
6. 更新能力矩阵和验证记录。只完成源码同步、解析或编译时，应保留尚未完成的运行验证项。

纯文档等低影响变更可沿用已有运行验证；依赖、原生工程、SDK、认证存储或数据库变化应增加相应检查。

### 当前可用命令

在仓库根目录执行，使用 Python 3.10+。脚本从 PowerShell 的 `PATH` 查找 Flutter OH，从环境变量或 `flutter config --ohos-sdk` 读取 HarmonyOS SDK；仓库不记录本机路径。Flutter / Dart 命令需要能够访问依赖源和 Flutter 资源。

```powershell
# 创建独立副本并严格验证锁文件；不会执行代码生成和 HAP 构建。
python ohos/tool/build_ohos.py --prepare-only

# 仅在有意调整依赖时执行：更新 ohos/flutter/pubspec.lock 后退出。
python ohos/tool/build_ohos.py --update-lockfile

# 适配完成后执行：严格解析、代码生成、本地化生成及 unsigned HAP 构建。
python ohos/tool/build_ohos.py --mode release
```

以上命令对应现有脚本，必要时仍可临时传入 `--flutter-sdk` 或 `--ohos-sdk`，但不得把本机路径提交到仓库。脚本目前不会自动执行 analyze、测试或真机安装；这些检查需要单独在相应 SDK 环境完成。SDK 身份验证和 Git 构建信息注入已经实现。

### 验证记录模板

将验证记录放在对应工作记录或 CI 结果中，链接到测试日志及产物，不在本文持续追加完整历史日志。

```text
验证日期：
源代码提交 / 未提交改动说明：
上游目标提交：
Flutter OH 正式 tag / framework commit / engine commit / Dart 版本：
DevEco / HarmonyOS SDK / Hvigor 版本：
鸿蒙覆盖配置及锁文件版本或摘要：
设备型号 / 系统版本：
严格依赖解析：未执行 / 通过 / 失败
双 SDK 代码生成、分析、测试：未执行 / 通过 / 失败
HAP 构建及包内版本、构建信息：未执行 / 通过 / 失败
真机冷启动、登录、课表与持久化：未执行 / 通过 / 失败
本次变更涉及的平台能力：
旧版本覆盖升级：未执行 / 通过 / 失败
剩余问题及复现步骤：
日志 / 构建产物位置：
```

## 8. 交付标准

- **首次可运行**：阶段一、二通过，开发环境中能启动、登录和使用课表，核心数据可持久化。
- **主要业务可用**：阶段三通过，新业务涉及的文件、图片和日历操作可用，旧用户升级验证通过。
- **功能追齐**：阶段四、五的能力逐项验收；平台限制有明确说明，未完成项不计为已支持。
- **持续跟随上游**：阶段六检查可重复运行，每次同步都有明确的源码、SDK、依赖和运行验证基线。
- **正式发布**：阶段七通过，签名 HAP 的安装和旧版本覆盖升级验证完成。

第三方插件接口兼容性和 WebView 行为是当前最大的未知项。先完成阶段一、二，再依据实际插件缺口细化后续工作量。
